using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClassAssign : MonoBehaviour
{
    // Start is called before the first frame update
    public int ClassNumber;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
